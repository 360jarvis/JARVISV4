import AppShell from '../../../components/AppShell';
import SettingsCenter from '../../../components/SettingsCenter';

export default function SettingsPage() {
  return <AppShell title="Settings" subtitle="Company setup, branding, business rules, payments, notifications, users, and portal controls."><SettingsCenter /></AppShell>;
}
