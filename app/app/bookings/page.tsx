import AppShell from '../../../components/AppShell';
import ModulePlaceholder from '../../../components/ModulePlaceholder';

export default function BookingsPage() {
  return <AppShell title="Booking Command Center" subtitle="Scheduled, recurring, quote-backed, canceled, and unassigned booking operations."><ModulePlaceholder name="Booking Command Center" /></AppShell>;
}
